#include<stdio.h>
int main()
{
	int a,b,i,even,odd;
	i=1;
	even=odd=0;
	a=20;
	printf("%d ",a);
	scanf("%d",&b);
	while(i<=a)
	{
		if(i%b==0)
		{
		printf("%d  ",i);
		if(i%2==0)
		even=even+i;
		else
		odd=odd+i;
	   }
	   i++;
	}
	printf("\n%d %d",even,odd);
}
